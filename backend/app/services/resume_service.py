"""
Resume Service
--------------
This is where the resume screening logic plugs in.
The team building resume screening should implement:
    - screen_resume(file_bytes: bytes, filename: str) -> dict

Expected return format:
    {
        "ats_score": 85.5,
        "structured_data": {
            "name": "...",
            "skills": [...],
            "experience": [...],
            "education": [...]
        }
    }
"""

# TODO: Replace this import with the actual resume screening module
# from resume_screening import screen_resume


def screen_resume(file_bytes: bytes, filename: str) -> dict:
    """
    PLACEHOLDER — swap this out with the real implementation.
    """
    raise NotImplementedError("Resume screening function not yet connected.")